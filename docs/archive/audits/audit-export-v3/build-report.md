# Build report — 2026-07-04

`npm run build` exit code: **0**

```
[build] 7 page(s) built in 1.35s
[build] Complete!
```

Routes generated: `/`, `/work/`, `/work/hera/`, `/work/enterprise-tools/`, `/resume/`, `/about/`, `/contact/`.

Note: rebuilding while the preview server is running fails with EPERM on Windows
(dist/ file locks). Stop preview -> build -> restart preview.
