# BUGS.md — Portfolio Bug Sheet

> Every real defect found during build, verification, or live-testing gets a row here — whether or not it was fixed in the same cycle. This is the durable record; a verifier/live-tester report is not enough on its own because reports don't get re-read later.
> Use the `/bug` skill to also file a GitHub issue when a bug warrants one; either way, the row below is mandatory.
> Do not delete closed rows — mark them Closed and keep the history.

| ID | Date | Found by | Slice/Page | Severity | Description | Status | Fix / Notes |
|---|---|---|---|---|---|---|---|
| BUG-001 | 2026-08-28 | Chief Coordinator (Claude) | `docs/agent-system/CURRENT-SPRINT.md` | Minor | File had literal unresolved Git merge-conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`) checked into row 0 of the sprint sequence table — a prior session's stash/merge was never resolved before committing. Didn't break the build (it's a doc, not code) but made the sprint map misleading. | Closed | Resolved by keeping both facts as separate rows: Public Release Split (closed 2026-08-19) and HERA Early Access Public Release (BUILT+VERIFIED), matching what the rest of the doc already said in prose. |
| BUG-002 | 2026-08-28 | portfolio-verifier (Claude) | `src/components/Nav.astro` | Minor | At emulated narrow widths (~460–480px CSS width, confirmed on both `/` and `/about/`), the brand-mark text span (`<span class="hidden sm:inline">HERA OS</span>`, Nav.astro line 28) rendered visibly and wrapped to two lines instead of being hidden by Tailwind's `sm:` (640px) breakpoint, pushing the nav pill wide enough that "Contact" (and part of "Lineage") scrolled off the right edge — real clipping of a nav destination. Pre-existing: the identical `hidden sm:inline` class already wrapped `{site.name}` ("Favour Diokpo", a *longer* string) before this slice, so this is not a regression introduced by the Slice 1 brand-mark swap — swapping in the shorter "HERA OS" text can only have made it marginally less bad. Verification confidence is moderate, not high: this session's Claude Browser pane also mis-reported requested vs. actual emulated viewport width twice during testing (asked for 375/390px, got back 481/463px), so a real-device or Playwright/Claude-in-Chrome recheck at an exact ≤430px width is recommended before treating this as fully confirmed. | Closed (verified fixed) | **Repair 2026-08-28 (repair builder):** re-diagnosed — the `hidden sm:inline` span was actually already correctly `display:none` at mobile (confirmed via computed-style query); the text visitors were seeing was the separate nav destination pill labeled "HERA OS" (`nav[0]` in `src/data/site.ts`, `{ label: 'HERA OS', href: '' }`, pre-dating this slice), which was wrapping to 2 lines because nav `<a>` labels had no `whitespace-nowrap`, AND the brand-mark `<picture>`/`<img>` was being flex-shrunk down to a ~8px sliver (not staying 28×28) because the outer brand `<a>` had no `shrink-0` inside `nav`'s `justify-between` row once total content exceeded the pill's width. Fixed in Nav.astro: added `shrink-0` to the brand `<a>`/`<picture>`/`<img>` so the logo holds its size; changed the nav-items `<div>` to `flex-1 min-w-0 overflow-x-auto` with `whitespace-nowrap shrink-0` on each link (scrollbar hidden via scoped `<style>`) so labels never wrap and every destination — including Contact — stays reachable via horizontal scroll instead of being clipped. Desktop (`sm:` and up, where content already fit) is visually unchanged — verified via screenshot. **Re-verified 2026-08-28 by the Chief directly at exactly 375px:** all 6 destinations reachable by scrolling the pill right, none clipped; desktop unaffected. Closing. |
| BUG-003 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence — Phase 2/3 clutter (mobile ~390-430px) | Minor | At mobile widths, the `.md`-file-pileup and panic-thought-line phase renders with the same item count/positions logic as desktop, just reflowed — not a deliberately reduced/paced mobile density. Result: filenames get clipped with an ellipsis in their pill labels (e.g. "CURRENT-S...", "decision-log...", "task-03-not..."), and panic-thought lines overlap other elements (e.g. "Wait. We already did this." sits on top of the AGENT response card corner; "Did I give it the right file?" overlaps the "HERA OS" section-label dot). Confirmed via live screenshots on a 390px-wide viewport, mid-animation. Landing state ("You lead. HERA keeps up." + logo) itself renders fine at this width. | Fixed (unverified) | **Repair 2026-08-28 (repair builder):** root cause was a CSS specificity bug, not missing mobile authoring — the `@media (max-width:640px)` block's `[data-file='N']`/`[data-thought='N']` position overrides were always dead code, because each item's `left`/`top` was set via an inline `style` attribute (from the desktop-authored `files`/`thoughts` arrays), and inline styles beat any class/attribute selector in a stylesheet regardless of media query. So mobile was silently rendering desktop's raw coordinates the whole time — confirmed empirically (e.g. `decision-log.md` rendered at its desktop `top:58%`, inside the AI-card band, not the mobile override's `top:72%`). Fixed by switching the inline styles to CSS custom properties (`--x`/`--y`), having the base `.md-file`/`.human-thought` rules read `left:var(--x); top:var(--y)`, and re-authoring the mobile override coordinates so all 5 files + 3 essential thoughts sit only in the empty bands above the headline/eyebrow and below the AI card (the card runs near-edge-to-edge at this width, so there's no clear space beside it, only above/below). Also fixed the ellipsis truncation: mobile `.md-file` pill widened 78px→92px and its `span` switched from `nowrap`+ellipsis to `white-space:normal; word-break:break-word` so long names (e.g. "CURRENT-SPRINT.md") wrap cleanly to 2 lines instead of truncating. Verified both by forcing full-clutter state (all items opacity:1, no animation) and by watching the real animated sequence at 390px and 430px — no overlaps with card/headline/eyebrow, no truncated names, `STOP MAKING NEW BRANCHES.` no longer clipped near the bottom edge. **Re-verified 2026-08-28 by the Chief directly at exactly 375px mid-animation:** all filenames fully legible in full (e.g. "CURRENT-SPRINT.md" wraps cleanly, no ellipsis), no overlaps with headline/card/nav. Closing. |
| BUG-004 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence — Phase 5 clear transition (laptop 1366px) | Cosmetic | Caught one transitional screenshot at 1366px where the landed headline "You lead. HERA keeps up." was already fully legible while several Phase-2/3 clutter items (files, red "STOP MAKING NEW BRANCHES.", other thought lines) were still faintly visible in the same frame, i.e. a brief crossfade rather than a purely decisive/hard cut. The very next screenshot (taken immediately after, no wait) showed a fully clean landed state, so the overlap window is short — likely within normal tolerance for "decisive," but noting since the spec explicitly calls out "not a fade." | Closed (superseded) | **Superseded and closed 2026-08-28 (Slice 1 repair).** The crossfade this described belonged to the autoplay choreography that has now been deleted outright. The replacement clear-to-HERA transition is scroll-controlled: the pain statement fades to a clean black field across p=0.93-0.95 and HERA only begins appearing from p=0.95. Independently verified in a real browser by the verifier - `hera-layer` opacity measured 0 for all p<0.95, with the pain layer fully faded before HERA starts, so there is no frame in which old and new content are simultaneously legible. Closing per the packet's instruction to close BUG-004 only once the new transition was visually proven. |

| BUG-005 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence — transition into video/demo section | Not a defect (invalid) | Real mouse-wheel scrolling (and keyboard `End`/`PageDown`) gets permanently stuck roughly 1,900-2,100px short of the true bottom of the page, landing inside/just past the video-demo section and never releasing - visitors cannot reach the loop-section, discoveries-section, or footer by normal scrolling. Reproduced with Playwright Chromium using real `page.mouse.wheel()` events (not `scrollTo`) across every viewport tested: 1440x900 (stuck at scrollY 10870 of 12807 max), 1366x768 (9286 of 11088), 1536x864 (10438 of 12339), 1440x1024 (12358 of 14419), and 1920x1080 (13030 of 15147) - consistent ~1,900px shortfall on all five. Kept applying up to 260 additional wheel ticks (78,000px of delta) with the mouse over the video, over empty page area, and away from any element - position stayed frozen. `End` key did nothing; 10x `PageDown` moved only 4px total. Even programmatic `window.scrollTo({top,...})` requests near the end of the page landed at inconsistent, non-monotonic positions short of the target (e.g. requesting 11000 landed at 10879; requesting 13000 landed at 12632), indicating an active scroll-position handler is fighting/reclamping scroll near the tail of the page rather than a simple max-height clamp. This matches the exact "visitors got permanently stuck" regression class called out in `docs/agent-system/BROWSER-TESTING-PROTOCOL.md`. | Closed — NOT A DEFECT | **Re-tested and refuted by the Chief 2026-08-28 with Playwright at 1440x900.** This is the pre-existing, intentional `#video-section` scroll gate in `src/pages/index.astro`, not a trap and not introduced by the Slice 1 repair. Two things caused the false positive. (1) In `mode === 'video'` every wheel event is `preventDefault()`ed and the ONLY forward exit is clicking the visible `#video-next` down-arrow button (`index.astro` ~line 1318, `videoNext?.addEventListener('click', () => goToLoop(0))`) — there is deliberately no downward wheel escape. The live tester never clicked it. Clicking it advances y=10870 -> 11770 immediately (button confirmed present, visible, 48x48 at x=696,y=744). (2) Past that gate, `#loop-section` steps through its 8 stages one gesture at a time behind a ~560ms `wheelLockUntil` throttle; wheel ticks fired faster than that are swallowed, which reads as "stuck". Wheeling at a human pace (~0.8s apart) walked loopStep 0->7 and then released to free scrolling, reaching y=12810 against a maximum of 12807 — i.e. the true bottom of the page. The 1,937px "shortfall" the live tester measured is exactly the height of the gated region. **Whether a wheel-only visitor should be required to click the arrow to continue is a legitimate UX question for Favour, but it is pre-existing behaviour outside this slice's file lane and is not a defect in the repair.** |
| BUG-006 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence - composer bubble at 375px mobile width | Minor | At exactly 375px viewport width, once the full line "Make a small change to the website" has finished typing into the composer input, the text runs to the right edge of the input field and is visually cut off/overlapped by the Send pill button - the last 1-2 characters ("e" of "website") sit behind/under the button rather than the field eliding or shrinking the button to keep them clear. Confirmed via screenshot; the same beat at 430px width was clean (full phrase fits with room to spare before the Send button), so this is specifically a 375px-width defect, not a universal mobile issue. | Closed (superseded) | **Superseded and closed 2026-08-29 (Slice 1 Fix 2).** The composer input, its typed sentence and the Send pill were deleted outright: the re-authored opening arrives with the request already sent, so there is no composer to collide with at any width. Confirmed absent by measurement at 1440/1366/390/375px — a live query for `.chat-window, .composer, .composer-send, .composer-input, .fake-cursor` returns 0 elements at every viewport (`docs/agent-system/screenshots/slice-1-fix-2/report.json`, `arrival.greyCardCount`). |

| BUG-007 | 2026-08-29 | Chief Coordinator (Claude) | Homepage opening sequence — arrival frame, all viewports | Major | The pinned cinematic stage started 69px below the top of the document, because the sticky `<header>` occupies that much real flow height before `<main>`. At `scrollY = 0` the 100svh stage therefore hung ~69px below the fold and clipped the "Scroll down" affordance — measured `cueBottom: 933` against a 900px viewport, i.e. the single element the packet requires to be legible on arrival was off-screen. Only the arrival frame was affected; once scrolled, the stage pinned correctly. | Closed (verified fixed) | Fixed in `src/components/OpeningFailureSequence.astro`: the section takes a negative top margin equal to the header's measured height (`--opening-nav-offset`, written at runtime on load/resize so it stays exact per breakpoint and after webfont swap), so the cinematic starts at the true top of the page and the floating nav pill sits over the black canvas. Re-measured at 1440x900, 1366x768, 390x844 and 375x812: `stageFillsViewport: true` and `cueFullyInViewport: true` at all four. |
| BUG-008 | 2026-08-29 | Chief Coordinator (Claude) | Homepage opening sequence — `.md` curtain | Major | The curtain passed its numeric coverage probe but failed visually: tiles were 38px icons in ~90px columns with 6px gaps, so the field read as scattered icons over open black sky rather than the continuous curtain the packet requires. The probe was measuring column *bounding boxes* (full-width, full-height) and so reported zero gaps while the rendered frame plainly showed them — a measurement that agreed with the spec while the picture did not. | Closed (verified fixed) | Fixed by sizing tiles as a fraction of their own column (`width: 78%`) instead of a fixed px, raising the column count 16→20 (12 at ≤900px, 7 at ≤640px), and cutting the inter-tile gap 6px→2px and padding 7px→4px. Re-shot at all four viewports: the upper band now reads as one mass, mean curtain depth 36.4–36.5% with a 27–46% spread across 20 distinct bottom edges. Screenshot: `slice-1-fix-2/desktop-1440x900/05-curtain-full.png`. |
| BUG-009 | 2026-08-29 | Chief Coordinator (Claude) | Homepage opening sequence — chat composition | Minor | With the chat column vertically centred, the curtain (which by design stops at ~36% viewport depth) came to rest just *above* the agent response instead of covering it, so the story beat "the paperwork buries the answer" did not land — the two elements barely overlapped. | Closed (verified fixed) | Fixed by anchoring the chat layer near the top (`align-items: flex-start` + `padding-top: clamp(88px, 13vh, 150px)`), which also matches how a real conversation grows downward. The curtain now covers the response's opening paragraph while the closing paragraph stays readable beneath it. |
| BUG-010 | 2026-08-29 | Chief Coordinator (Claude) | Homepage opening sequence — panic card pile | Minor | The accumulating pile rendered every card's text at its own opacity, so up to three cards' sentences overlapped in the peek area above the front card and read as a rendering fault rather than a stack of paper. The pile also read as ~4 cards rather than 8, because the 12px/5px offsets and a fast opacity falloff merged the rear edges. | Closed (verified fixed) | Fixed by fading each card's *text* independently of its body (`--card-text`, reaching 0 by one card of depth) so only the front card is readable, adding a hairline border for edge separation, slowing the opacity falloff (min 0.32), and widening the offsets to 16px/7px (trimmed to 12px/3px at ≤640px so an 86vw card cannot be pushed off the left edge). All 8 cards now measure visible with 8 distinct offsets and no clipping at any viewport. |
| BUG-011 | 2026-08-29 | Chief Coordinator (Claude) | `/work/lineage/` | Minor | The Lineage route requests `videos/lifestream.mp4`, which does not exist in `public/` — the page logs a 404 on every load. Pre-existing and entirely outside this slice's file lane; not introduced by Slice 1 Fix 2 and deliberately not fixed here per the packet's "do not fix unrelated pre-existing errors" boundary. | Open | Found incidentally during the Fix 2 regression sweep (`slice-1-fix-2/regression.json`). Either add the asset to `public/videos/` or remove the `<video>` source on the Lineage page. Needs a decision about whether that video should exist at all. |

**Severity:** Blocker (breaks the build or a core flow) · Major (visibly wrong, ships broken if unfixed) · Minor (polish, pacing, edge case) · Cosmetic

**Status:** Open · In repair · Fixed (unverified) · Closed (verified fixed) · Won't fix (with reason)

---

*File created 2026-08-28 as part of the Claude Code portfolio workflow setup. Maintained by whichever agent finds or fixes a bug — update it even when there's nothing new, so its last-touched date stays a true signal.*
