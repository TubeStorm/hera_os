# BUGS.md — Portfolio Bug Sheet

> Every real defect found during build, verification, or live-testing gets a row here — whether or not it was fixed in the same cycle. This is the durable record; a verifier/live-tester report is not enough on its own because reports don't get re-read later.
> Use the `/bug` skill to also file a GitHub issue when a bug warrants one; either way, the row below is mandatory.
> Do not delete closed rows — mark them Closed and keep the history.

| ID | Date | Found by | Slice/Page | Severity | Description | Status | Fix / Notes |
|---|---|---|---|---|---|---|---|
| BUG-001 | 2026-08-28 | Chief Coordinator (Claude) | `docs/agent-system/CURRENT-SPRINT.md` | Minor | File had literal unresolved Git merge-conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`) checked into row 0 of the sprint sequence table — a prior session's stash/merge was never resolved before committing. Didn't break the build (it's a doc, not code) but made the sprint map misleading. | Closed | Resolved by keeping both facts as separate rows: Public Release Split (closed 2026-08-19) and HERA Early Access Public Release (BUILT+VERIFIED), matching what the rest of the doc already said in prose. |
| BUG-002 | 2026-08-28 | portfolio-verifier (Claude) | `src/components/Nav.astro` | Minor | At emulated narrow widths (~460–480px CSS width, confirmed on both `/` and `/about/`), the brand-mark text span (`<span class="hidden sm:inline">HERA OS</span>`, Nav.astro line 28) rendered visibly and wrapped to two lines instead of being hidden by Tailwind's `sm:` (640px) breakpoint, pushing the nav pill wide enough that "Contact" (and part of "Lineage") scrolled off the right edge — real clipping of a nav destination. Pre-existing: the identical `hidden sm:inline` class already wrapped `{site.name}` ("Favour Diokpo", a *longer* string) before this slice, so this is not a regression introduced by the Slice 1 brand-mark swap — swapping in the shorter "HERA OS" text can only have made it marginally less bad. Verification confidence is moderate, not high: this session's Claude Browser pane also mis-reported requested vs. actual emulated viewport width twice during testing (asked for 375/390px, got back 481/463px), so a real-device or Playwright/Claude-in-Chrome recheck at an exact ≤430px width is recommended before treating this as fully confirmed. | Closed (verified fixed) | **Repair 2026-08-28 (repair builder):** re-diagnosed — the `hidden sm:inline` span was actually already correctly `display:none` at mobile (confirmed via computed-style query); the text visitors were seeing was the separate nav destination pill labeled "HERA OS" (`nav[0]` in `src/data/site.ts`, `{ label: 'HERA OS', href: '' }`, pre-dating this slice), which was wrapping to 2 lines because nav `<a>` labels had no `whitespace-nowrap`, AND the brand-mark `<picture>`/`<img>` was being flex-shrunk down to a ~8px sliver (not staying 28×28) because the outer brand `<a>` had no `shrink-0` inside `nav`'s `justify-between` row once total content exceeded the pill's width. Fixed in Nav.astro: added `shrink-0` to the brand `<a>`/`<picture>`/`<img>` so the logo holds its size; changed the nav-items `<div>` to `flex-1 min-w-0 overflow-x-auto` with `whitespace-nowrap shrink-0` on each link (scrollbar hidden via scoped `<style>`) so labels never wrap and every destination — including Contact — stays reachable via horizontal scroll instead of being clipped. Desktop (`sm:` and up, where content already fit) is visually unchanged — verified via screenshot. **Re-verified 2026-08-28 by the Chief directly at exactly 375px:** all 6 destinations reachable by scrolling the pill right, none clipped; desktop unaffected. Closing. |
| BUG-003 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence — Phase 2/3 clutter (mobile ~390-430px) | Minor | At mobile widths, the `.md`-file-pileup and panic-thought-line phase renders with the same item count/positions logic as desktop, just reflowed — not a deliberately reduced/paced mobile density. Result: filenames get clipped with an ellipsis in their pill labels (e.g. "CURRENT-S...", "decision-log...", "task-03-not..."), and panic-thought lines overlap other elements (e.g. "Wait. We already did this." sits on top of the AGENT response card corner; "Did I give it the right file?" overlaps the "HERA OS" section-label dot). Confirmed via live screenshots on a 390px-wide viewport, mid-animation. Landing state ("You lead. HERA keeps up." + logo) itself renders fine at this width. | Fixed (unverified) | **Repair 2026-08-28 (repair builder):** root cause was a CSS specificity bug, not missing mobile authoring — the `@media (max-width:640px)` block's `[data-file='N']`/`[data-thought='N']` position overrides were always dead code, because each item's `left`/`top` was set via an inline `style` attribute (from the desktop-authored `files`/`thoughts` arrays), and inline styles beat any class/attribute selector in a stylesheet regardless of media query. So mobile was silently rendering desktop's raw coordinates the whole time — confirmed empirically (e.g. `decision-log.md` rendered at its desktop `top:58%`, inside the AI-card band, not the mobile override's `top:72%`). Fixed by switching the inline styles to CSS custom properties (`--x`/`--y`), having the base `.md-file`/`.human-thought` rules read `left:var(--x); top:var(--y)`, and re-authoring the mobile override coordinates so all 5 files + 3 essential thoughts sit only in the empty bands above the headline/eyebrow and below the AI card (the card runs near-edge-to-edge at this width, so there's no clear space beside it, only above/below). Also fixed the ellipsis truncation: mobile `.md-file` pill widened 78px→92px and its `span` switched from `nowrap`+ellipsis to `white-space:normal; word-break:break-word` so long names (e.g. "CURRENT-SPRINT.md") wrap cleanly to 2 lines instead of truncating. Verified both by forcing full-clutter state (all items opacity:1, no animation) and by watching the real animated sequence at 390px and 430px — no overlaps with card/headline/eyebrow, no truncated names, `STOP MAKING NEW BRANCHES.` no longer clipped near the bottom edge. **Re-verified 2026-08-28 by the Chief directly at exactly 375px mid-animation:** all filenames fully legible in full (e.g. "CURRENT-SPRINT.md" wraps cleanly, no ellipsis), no overlaps with headline/card/nav. Closing. |
| BUG-004 | 2026-08-28 | portfolio-live-tester (Claude) | Homepage opening sequence — Phase 5 clear transition (laptop 1366px) | Cosmetic | Caught one transitional screenshot at 1366px where the landed headline "You lead. HERA keeps up." was already fully legible while several Phase-2/3 clutter items (files, red "STOP MAKING NEW BRANCHES.", other thought lines) were still faintly visible in the same frame, i.e. a brief crossfade rather than a purely decisive/hard cut. The very next screenshot (taken immediately after, no wait) showed a fully clean landed state, so the overlap window is short — likely within normal tolerance for "decisive," but noting since the spec explicitly calls out "not a fade." | Open | Not fixed this session — worth a quick look at the clutter-exit transition's timing/easing to confirm it's a snap/wipe rather than an opacity crossfade with the incoming logo group. |

**Severity:** Blocker (breaks the build or a core flow) · Major (visibly wrong, ships broken if unfixed) · Minor (polish, pacing, edge case) · Cosmetic

**Status:** Open · In repair · Fixed (unverified) · Closed (verified fixed) · Won't fix (with reason)

---

*File created 2026-08-28 as part of the Claude Code portfolio workflow setup. Maintained by whichever agent finds or fixes a bug — update it even when there's nothing new, so its last-touched date stays a true signal.*
